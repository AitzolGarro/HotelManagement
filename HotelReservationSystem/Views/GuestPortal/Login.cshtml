@{
    Layout = "~/Views/Shared/_GuestPortalLayout.cshtml";
    ViewData["Title"] = L["GP_Login_Title"].Value;
}

<div class="min-vh-100 d-flex align-items-center justify-content-center" style="background: linear-gradient(135deg, #0d6efd 0%, #0a58ca 100%); margin: -24px;">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-12 col-sm-10 col-md-7 col-lg-5 col-xl-4">

                <!-- Logo / Brand -->
                <div class="text-center mb-4">
                    <div class="bg-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width:72px;height:72px;box-shadow:0 4px 20px rgba(0,0,0,.15)">
                        <i class="bi bi-building text-primary fs-2"></i>
                    </div>
                    <h1 class="text-white fw-bold fs-3 mb-1">@L["GP_GuestPortal"]</h1>
                    <p class="text-white-50 mb-0">@L["GP_Login_Subtitle"]</p>
                </div>

                <!-- Login Card -->
                <div class="card border-0 shadow-lg" style="border-radius:16px">
                    <div class="card-body p-4 p-md-5">
                        <h2 class="fs-5 fw-semibold mb-1">@L["GP_Login_WelcomeBack"]</h2>
                        <p class="text-muted small mb-4">@L["GP_Login_Desc"]</p>

                        <div id="loginError" class="alert alert-danger d-none" role="alert">
                            <i class="bi bi-exclamation-triangle-fill me-2"></i>
                            <span id="loginErrorMsg">@L["GP_Login_InvalidCredentials"]</span>
                        </div>

                        <form id="guestLoginForm" novalidate>
                            <div class="mb-3">
                                <label for="loginEmail" class="form-label fw-medium">@L["GP_Login_Email"]</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light border-end-0">
                                        <i class="bi bi-envelope text-muted"></i>
                                    </span>
                                    <input type="email" class="form-control border-start-0 ps-0" id="loginEmail"
                                           placeholder="your@email.com" required autocomplete="email" />
                                </div>
                                <div class="invalid-feedback">@L["GP_Login_EmailRequired"]</div>
                            </div>

                            <div class="mb-4">
                                <label for="loginBookingRef" class="form-label fw-medium">@L["GP_Login_BookingRef"]</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light border-end-0">
                                        <i class="bi bi-hash text-muted"></i>
                                    </span>
                                    <input type="text" class="form-control border-start-0 ps-0 text-uppercase" id="loginBookingRef"
                                           placeholder="e.g. BK-2024-001" required autocomplete="off"
                                           style="letter-spacing:.05em" />
                                </div>
                                <div class="form-text">@L["GP_Login_BookingRefHint"]</div>
                                <div class="invalid-feedback">@L["GP_Login_RefRequired"]</div>
                            </div>

                            <button type="submit" class="btn btn-primary w-100 py-2 fw-semibold" id="loginBtn">
                                <span id="loginBtnText"><i class="bi bi-box-arrow-in-right me-2"></i>@L["GP_Login_SignIn"]</span>
                                <span id="loginBtnSpinner" class="d-none">
                                    <span class="spinner-border spinner-border-sm me-2" role="status"></span>@L["GP_Login_SigningIn"]
                                </span>
                            </button>
                        </form>

                        <hr class="my-4">
                        <p class="text-center text-muted small mb-0">
                            <i class="bi bi-shield-lock me-1"></i>
                            @L["GP_Login_DataProtected"]
                        </p>
                    </div>
                </div>

                <p class="text-center text-white-50 small mt-4">
                    @L["GP_Login_NeedHelp"] <a href="mailto:support@hotel.com" class="text-white">support</a>
                </p>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script>
(function () {
    // If already logged in, redirect to dashboard
    if (localStorage.getItem('guestToken')) {
        window.location.href = '/GuestPortal/Dashboard';
        return;
    }

    const form = document.getElementById('guestLoginForm');
    const errorBox = document.getElementById('loginError');
    const errorMsg = document.getElementById('loginErrorMsg');
    const btnText = document.getElementById('loginBtnText');
    const btnSpinner = document.getElementById('loginBtnSpinner');

    form.addEventListener('submit', async (e) => {
        e.preventDefault();
        if (!form.checkValidity()) { form.classList.add('was-validated'); return; }

        const email = document.getElementById('loginEmail').value.trim();
        const bookingRef = document.getElementById('loginBookingRef').value.trim().toUpperCase();

        btnText.classList.add('d-none');
        btnSpinner.classList.remove('d-none');
        errorBox.classList.add('d-none');

        try {
            const res = await fetch('/api/guest-portal/login', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email, bookingReference: bookingRef })
            });

            const data = await res.json();

            if (!res.ok) {
                errorMsg.textContent = data.message || '@Html.Raw(L["GP_Login_InvalidCredentials"].Value)';
                errorBox.classList.remove('d-none');
                return;
            }

            // Store token and guest info
            localStorage.setItem('guestToken', data.token);
            localStorage.setItem('guestExpires', data.expires);
            localStorage.setItem('guestProfile', JSON.stringify(data.guest));

            const returnUrl = new URLSearchParams(window.location.search).get('returnUrl');
            window.location.href = returnUrl || '/GuestPortal/Dashboard';

        } catch (err) {
            errorMsg.textContent = '@Html.Raw(L["GP_Login_NetworkError"].Value)';
            errorBox.classList.remove('d-none');
        } finally {
            btnText.classList.remove('d-none');
            btnSpinner.classList.add('d-none');
        }
    });
})();
</script>
}
