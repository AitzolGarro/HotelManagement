# Environment variables for local development.
# In production (Coolify), all variables are injected via the Coolify dashboard.
# Do NOT commit real secrets here.

# Example variables (override in Coolify environment settings):
# ASPNETCORE_ENVIRONMENT=Production
# ConnectionStrings__DefaultConnection=
# Jwt__Secret=
# Stripe__SecretKey=
